<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The settings screen is a handful of stock widgets, so the platform theme is enough. -->
    <style name="Theme.UvWidget" parent="@android:style/Theme.DeviceDefault.Light" />
</resources>
